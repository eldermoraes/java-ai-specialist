# ─── Modelo (Ollama Cloud) ───────────────────────────────────────────────────
# Um modelo só nesta aula: quem trabalha são os guardrails, que rodam depois que a
# resposta chega. Temperatura baixa para a resposta ficar estável entre uma execução
# e outra durante a demonstração.
quarkus.langchain4j.ollama.chat-model.model-id=deepseek-v4-pro:cloud
quarkus.langchain4j.ollama.chat-model.temperature=0.2
quarkus.langchain4j.timeout=180s

# Rodando com Ollama local, troque a linha acima por um modelo que você tenha baixado:
# quarkus.langchain4j.ollama.chat-model.model-id=qwen3:4b

# Usamos o Ollama já instalado (localhost:11434 / Ollama Cloud); sem dev services.
quarkus.langchain4j.ollama.devservices.enabled=false
quarkus.langchain4j.devservices.preload=false

# ─── Limite de tentativas ────────────────────────────────────────────────────
# Quantas vezes a chamada ao modelo pode acontecer numa mesma requisição quando um
# guardrail pede retry ou reprompt. Dois é a chamada original mais uma tentativa.
# Zero desliga a tentativa extra: a primeira reprovação já vira exceção.
# A quarta requisição do README troca este valor por 0 com o dev mode no ar.
quarkus.langchain4j.guardrails.max-retries=2

# ─── Log ─────────────────────────────────────────────────────────────────────
# Cada guardrail registra a decisão que tomou e o motivo. Parte do seu estudo é ler
# esse log: é ali que dá para ver a fila rodando em ordem e parando onde parou.
quarkus.log.category."com.eldermoraes.guardrails".level=DEBUG

# Log de cada request/response ao modelo. É no request da segunda chamada que aparece
# a mensagem do reprompt, com o motivo da reprovação dentro da conversa.
quarkus.langchain4j.log-requests=true
quarkus.langchain4j.log-responses=true
